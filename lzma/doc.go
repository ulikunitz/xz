// Package lzma procides capabilities to compress into decompress from LZMA and
// LZMA2 streams. The LZMA stream is uninterrupted range encoded stream and
// LZMA2 splits the data into chunks to allow parallel generation and reading.
package lzma
